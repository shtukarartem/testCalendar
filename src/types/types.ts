export type SelectOptions = {
  title: string;
  value: string;
};

export type RoomType = 'workPlace' | 'office' | 'meeting';

export type ViewListType = {
  icon:string,
  title: string,
  isPinned: boolean
}